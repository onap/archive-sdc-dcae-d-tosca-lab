tosca_definitions_version: tosca_simple_yaml_1_0_0
metadata:
  template_name: policy
imports:
- schema: schema.yaml
topology_template:

  node_templates:
    policy:
      type: tosca.dcae.nodes.policy
